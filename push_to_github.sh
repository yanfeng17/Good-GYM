# 推送代码到 GitHub（在创建仓库后运行）
cd "c:\AI Coding\Good-GYM"
git push -u origin master

# 如果遇到权限问题，可能需要设置 SSH 密钥或使用 HTTPS
# HTTPS 方式：
# git remote set-url origin https://github.com/yanfeng17/Good-GYM.git
# git push -u origin master
